<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper.java의 풀 패키지명 + 클래스명 -->
<mapper namespace="com.winter.app.review.ReviewMapper">
	<insert id="create" parameterType="ReviewDTO">
		INSERT INTO "TB_REVIEW"
		VALUES (default, #{username}, #{reviewContents}, #{reviewStar}, now(), #{productNum})
	</insert>
	
	<select id="list" parameterType="ReviewDTO" resultType="ReviewDTO">
		SELECT * FROM "TB_REVIEW"
		WHERE "PRODUCT_NUM"=#{productNum}
		ORDER BY "REVIEW_NUM" DESC
	</select>
	
	<delete id="delete" parameterType="ReviewDTO">
		DELETE FROM "TB_REVIEW"
		WHERE "REVIEW_NUM"=#{reviewNum}
	</delete>
	
	<update id="update" parameterType="ReviewDTO">
		UPDATE "TB_REVIEW"
 			SET "REVIEW_CONTENTS" = #{reviewContents}
 		WHERE "REVIEW_NUM" = #{reviewNum}
	</update>
	
</mapper>