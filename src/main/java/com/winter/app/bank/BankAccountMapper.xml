<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.winter.app.bank.BankAccountMapper">
	
	<select id="list" parameterType="bankAccountDTO" resultType="BankAccountDTO">
		SELECT * FROM "TB_BANKACCOUNT"
		ORDER BY "ACCOUNT_NUM" DESC 
	</select>
	
	
</mapper>